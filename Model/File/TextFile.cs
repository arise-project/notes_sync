namespace notes_sync.Model
{
    public class TextFile : FileModel
    {
        public TextFile(string fullName, string folder, string text)
            : base(fullName, folder, text)
        {

        }
    }
}
